[package]
name = "compilation-time"
version = "0.1.0"
edition = "2021"

[dependencies]
wasmer = { version = "7.0.1", features = ["singlepass", "cranelift"] }
wasmtime = { version = "42.0.1", features = ["winch"] }
walkdir = "2.5.0"
